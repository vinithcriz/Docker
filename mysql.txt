 docker run -d --name mysqldb -e MYSQL_ROOT_USER=admin -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=mydatabase -p 3306:3306 mysql
 docker exec -it mysql bash
 mysql -u root -ppassword

show databases;

show tables;

CREATE TABLE employees (
    id INT ,
    name VARCHAR(100),
    email VARCHAR(100),
    age INT
);

INSERT INTO employees (name, email, age)
VALUES 
('jeorge', 'jeorge@example.com', 25),
('Bob', 'bob@example.com', 32),
('Criz', 'criz@example.com', 29);

show tables;

exit
exit
